{
  "schema_version": "0.1.0",
  "description": "No source URLs were supplied for the Phase 0-1 bootstrap run. Add entries without changing PENDING_CHECK until access is actually attempted.",
  "sources": []
}
